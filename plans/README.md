# Implementation Plans

Generated by the Improve skill on 2026-07-21 at commit `41f59cd`. Execute in
the order below. Ollin Check owns worktree, branch, delivery, and status
metadata; executors must not edit this index.

## Execution order and status

| Plan | Title | Priority | Effort | Depends on | Status |
| --- | --- | --- | --- | --- | --- |
| 001 | Preserve real pane lookup failures | P1 | S | - | PR READY |
| 002 | Restyle and harden the tmux help dialog | P1 | M | - | PR READY |

Status values for Ollin delivery: `TODO`, `IN PROGRESS`, `READY FOR PR`,
`PR DRAFT`, `PR READY`, `DONE`, `BLOCKED`, or `REJECTED`.

## Routing state

- **Routing profile**: `gpt-5.6-sol@high=100%`
- **Effective routing**: `gpt-5.6-sol@high=100%`
- **Harness**: authenticated Codex CLI (`codex`)
- **Plan 001 assignment**: `gpt-5.6-sol`, level `high`
- **Plan 002 assignment**: `gpt-5.6-sol`, level `high`

## Dependency notes

- The plans have no source-level dependency, but Ollin must execute them in
  order as one linear PR stack.
- Plan 001 starts from `chore/pane-navigation-hints`.
- Plan 002 starts only after Plan 001 reaches `PR READY`, and its draft PR uses
  the Plan 001 branch as its base.

## Findings considered and rejected

- Rewriting the complete plugin core error model: disproportionate to the
  confirmed `getPane` bug and inconsistent with this focused branch.
- Changing `closePluginPane` from its intentional boolean contract: unrelated
  to pane lookup semantics.
- Adding scrolling, Enter-to-close, or new tmux commands: the supplied image is
  a visual target, not authorization to add interactions or change the
  command catalogue.
- Adding a terminal styling dependency: the required renderer is small enough
  to keep dependency-free with local ANSI helpers.
- Adding interfaces, ports, adapters, `Record` types, reflection, `typeof`, or
  `instanceof` to support these changes: none is required. New render data is
  represented by immutable class DTOs/value objects, one exported class per
  file, with concrete collaborators split by concern.
- Broad refactors in tab numbers, config editing, or pane navigation hints:
  the existing verification baseline passes and no additional high-confidence,
  high-leverage defect was confirmed.

## Binding implementation constraints

- These constraints apply to all newly added or modified logic. Existing
  unrelated legacy declarations are not authorization for a repository-wide
  rewrite.
- Use classes for behavior and immutable class DTOs/value objects for new data
  structures. Keep one exported class per file.
- Do not add `typeof`, `instanceof`, `Record`, `Reflect`, metadata reflection,
  dynamic object-key inspection, interfaces, or adapter layers.
- Split styling, terminal-line layout, and test-output inspection into focused
  files. Do not grow `BindingsPanelRenderer` into a god class.
- Apply SOLID principles only where they reduce coupling or clarify ownership;
  do not manufacture ports or indirection for convention's sake.
